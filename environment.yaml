name: voicetype
channels:
  - conda-forge
dependencies:
  - python
  - fastapi>=0.93.0
  - uvicorn
  - pyaudio
  - pynput
  - speechrecognition
  # needed by faster-whisper gpu
  - libcublas
  - cudnn
  # whisper
  - pysoundfile
  # dev
  - ruff
  - pre-commit
  # pip
  - pip
  - pip:
    # whisper
    - openai-whisper
    - faster-whisper
variables:
  VAR1: valueA
  VAR2: valueB
